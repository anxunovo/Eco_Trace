## Known Bugs

### Accessibility
- [x] Missing `<main>` landmark element across pages.
- [x] Insufficient color contrast on the bottom navigation bar (`.tab-item`).
- [x] Insufficient color contrast on "已过期" labels (`.bg-red-500`).
- [x] Viewport `user-scalable=no` prevents zooming on mobile devices.
